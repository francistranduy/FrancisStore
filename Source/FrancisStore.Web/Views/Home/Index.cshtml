@{
    ViewBag.Title = "Francis Store";
}

<!-- Slide -->
@{Html.RenderAction("Slide", "Home");}

<!-- Banner -->
@{Html.RenderAction("Banner", "Home");}

<!-- Our product -->
@{Html.RenderAction("OurProduct", "Home");}

<!-- Banner video -->
@{Html.RenderAction("BannerVideo", "Home");}

<!-- Blog -->
@{Html.RenderAction("Blog", "Home");}

<!-- Instagram -->
@{Html.RenderAction("Instagram", "Home");}

<!-- Shipping -->
@{Html.RenderAction("Shipping", "Home");}

@section DropdownSelection
    {
    <div id="languegeSelection"></div>
}

@section ModalVideo
    {
    <div class="modal fade" id="modal-video-01" tabindex="-1" role="dialog" aria-hidden="true">

        <div class="modal-dialog" role="document" data-dismiss="modal">
            <div class="close-mo-video-01 trans-0-4" data-dismiss="modal" aria-label="Close">&times;</div>

            <div class="wrap-video-mo-01">
                <div class="w-full wrap-pic-w op-0-0"><img src="Images/icons/video-16-9.jpg" alt="IMG"></div>
                <div class="video-mo-01">
                    <iframe src="https://www.youtube.com/embed/Nt8ZrWY2Cmk?rel=0&amp;showinfo=0" allowfullscreen></iframe>
                </div>
            </div>
        </div>
    </div>
}

@section Scripts
    {
    <script type="text/javascript">
        //Parallax for Banner Video
        $('.parallax100').parallax100();

        //Dropdown for Language Selection
        $(".selection-1").select2({
            minimumResultsForSearch: 20,
            dropdownParent: $('#languegeSelection')
        });

        //Add to cart modal
        $('.block2-btn-addcart').each(function () {
            var nameProduct = $(this).parent().parent().parent().find('.block2-name').html();
            $(this).on('click', function () {
                swal(nameProduct, "is added to cart !", "success");
            });
        });

        //Add to cart modal
        $('.block2-btn-addwishlist').each(function () {
            var nameProduct = $(this).parent().parent().parent().find('.block2-name').html();
            $(this).on('click', function () {
                swal(nameProduct, "is added to wishlist !", "success");
            });
        });
    </script>
}